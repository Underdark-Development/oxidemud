pre-commit:
  parallel: false
  commands:
    fmt:
      run: |
        just fmt
        git add {staged_files}
    clippy:
      run: just lint
    test:
      run: just test-staged {staged_files}

commit-msg:
  commands:
    verify:
      run: cog verify --file {1}
